# 📱 企业微信群机器人接入指南

## 🎯 权限要求

**好消息：几乎零门槛！**

- ❌ 无需企业认证
- ❌ 无需管理员权限
- ✅ 任何群成员都可以添加
- ✅ 完全免费

## 📋 快速接入步骤

### 第一步：创建群机器人

**重要提示：** 群机器人入口可能因企业微信版本而异，请按以下方法尝试：

#### 方法1：PC端（Windows/Mac）- 推荐

1. **打开企业微信PC客户端**，进入任意群聊
2. 点击群聊窗口右上角的 `···` (三个点) 或群名称
3. 在弹出的菜单/面板中找到 `群机器人` 选项
   - 如果看不到，尝试向下滚动查找
   - 某些版本可能显示为 `机器人` 或 `添加机器人`
4. 点击 `添加机器人` 或 `添加群机器人`
5. 设置机器人名称（比如：AI新闻助手）
6. 选择头像（可选）
7. 点击 `添加` 或 `完成`

#### 方法2：手机端（iOS/Android）

1. **打开企业微信App**，进入群聊
2. 点击右上角 `···` (更多) 或群名称
3. 在群设置页面，向下滚动找到 `群机器人` 选项
   - 位置可能在：群管理、群设置、更多功能等区域
4. 点击进入后，选择 `添加机器人`
5. 设置名称和头像
6. 点击 `完成`

#### 方法3：通过群管理（如果以上方法找不到）

1. 进入群聊
2. 点击群名称 → `群设置`
3. 找到 `群管理` 或 `高级设置`
4. 在其中查找 `群机器人` 或 `机器人管理`

#### 如果还是找不到？

可能的原因和解决方案：

**原因1：权限限制**
- 某些企业可能禁用了群机器人功能
- 解决：联系企业微信管理员确认是否开启了群机器人权限

**原因2：群聊类型限制**
- 单人群（只有你一个人）可能无法添加机器人
- 解决：创建一个包含至少2人的群聊

**原因3：企业微信版本过旧**
- 旧版本可能不支持群机器人
- 解决：更新到最新版本
  - PC端：帮助 → 检查更新
  - 手机端：应用商店更新

**原因4：使用的是个人微信而非企业微信**
- 个人微信没有群机器人功能
- 解决：确认使用的是企业微信（WeCom），不是微信（WeChat）

**原因5：企业管理员限制了创建权限（⭐ 最常见）**
- 管理员在后台开启了"限制创建机器人"
- 只有白名单成员才能创建
- 解决方案：
  1. **联系企业微信管理员**，请求加入白名单
  2. **或请有权限的同事**帮忙创建，获取Webhook URL后你也能用
  3. **或改用Telegram**：[Telegram快速开始](TELEGRAM_QUICKSTART.md)

#### 💡 重要提示

**即使你不能创建机器人，也能使用！**

只要有人在群里创建了机器人并分享 Webhook URL，任何人都可以用这个URL推送消息。

**解决办法**：
1. 找一个有权限的同事
2. 请他在群里添加机器人
3. 复制 Webhook URL 给你
4. 你就可以用这个URL运行 `bot_wecom.py` 了

#### 📸 视觉参考

群机器人入口通常长这样：

**PC端**：
```
┌─────────────────────────────┐
│  群聊设置                    │
├─────────────────────────────┤
│  群公告                      │
│  群文件                      │
│  群相册                      │
│  ⭐ 群机器人  ← 看这里      │
│  群管理                      │
│  ...                         │
└─────────────────────────────┘
```

**手机端**：
```
┌─────────────────────────────┐
│  群聊信息                    │
├─────────────────────────────┤
│  群成员 (3)                  │
│  群聊名称                    │
│  群公告                      │
│  查找聊天记录                │
│  ⭐ 群机器人  ← 看这里      │
│  ...                         │
└─────────────────────────────┘
```

### 第二步：获取Webhook URL

添加成功后，会显示一个 **Webhook地址**，类似这样：

```
https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=693a91f6-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

📋 **复制这个URL，待会要用！**

### 第三步：配置环境变量

1. 复制 `.env.example` 为 `.env`：
   ```bash
   cp .env.example .env
   ```

2. 编辑 `.env` 文件，填入刚才复制的Webhook URL：
   ```env
   # 企业微信机器人配置
   WECOM_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=你的key

   # DeepSeek API配置
   DEEPSEEK_API_KEY=你的DeepSeek_API_Key
   ```

### 第四步：运行机器人

```bash
# 安装依赖（如果还没安装）
pip install -r requirements.txt

# 运行企业微信版本
python bot_wecom.py
```

## 🚀 使用方式

### 方式1：命令行运行（手动触发）

```bash
# 发送AI新闻
python bot_wecom.py

# 修改代码发送其他主题
# 编辑 bot_wecom.py 的 main() 函数
```

### 方式2：定时推送（自动化）

#### macOS/Linux - 使用 crontab

1. 编辑 crontab：
   ```bash
   crontab -e
   ```

2. 添加定时任务（每天早上9点推送AI新闻）：
   ```cron
   0 9 * * * cd /path/to/ai-news-bot && python bot_wecom.py
   ```

3. 推送多个主题（修改 `bot_wecom.py`）：
   ```python
   # 在 main() 函数中，改成：
   send_daily_news(['ai', 'finance', 'startup'])
   ```

#### Windows - 使用任务计划程序

1. 打开 `任务计划程序`
2. 创建基本任务
3. 设置触发器（每天特定时间）
4. 操作：运行程序 `python`
5. 参数：`bot_wecom.py`
6. 起始位置：`C:\path\to\ai-news-bot`

## ⚙️ 高级配置

### 自定义发送的主题

编辑 `bot_wecom.py` 的 `main()` 函数：

```python
def main():
    # ... 前面的代码 ...

    # 方法1: 发送单个主题
    news = get_news('ai')
    send_wecom_message(news)

    # 方法2: 发送多个主题
    send_daily_news(['ai', 'finance', 'startup'])

    # 方法3: 发送所有主题
    send_daily_news()  # 不传参数，默认发送所有主题
```

### 发送格式

企业微信支持两种消息格式：

1. **纯文本**（当前使用）
   ```python
   send_wecom_message(content, msgtype="text")
   ```

2. **Markdown格式**
   ```python
   send_wecom_message(content, msgtype="markdown")
   ```

## 📊 使用限制

企业微信群机器人有以下限制：

- 🔄 **频率限制**：每个机器人每分钟最多发送 20 条消息
- 📝 **文本长度**：单条消息最长 2048 字节（约 680 个汉字）
- 🚫 **功能限制**：
  - 只能主动发送，无法接收消息
  - 无法 @ 特定成员
  - 仅在添加的群内有效

## 💡 实用技巧

### 1. 多群推送

如果想在多个群推送新闻：

1. 在每个群分别添加机器人
2. 获取多个 Webhook URL
3. 修改代码循环发送：

```python
WEBHOOK_URLS = [
    "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=群1的key",
    "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=群2的key",
    "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=群3的key",
]

for webhook_url in WEBHOOK_URLS:
    WECOM_WEBHOOK_URL = webhook_url
    send_daily_news(['ai'])
```

### 2. 错误通知

如果想在发送失败时收到通知，可以添加异常处理：

```python
try:
    send_daily_news(['ai'])
except Exception as e:
    # 发送错误通知到另一个群
    error_msg = f"⚠️ 新闻推送失败：{str(e)}"
    send_wecom_message(error_msg)
```

### 3. 测试消息

在正式运行前，可以先发送测试消息：

```python
send_wecom_message("🤖 测试消息：机器人已成功连接！")
```

## 🔍 常见问题

### Q: Webhook URL在哪里查看？

A: 打开企业微信群 → 右上角 `···` → `群机器人` → 点击已添加的机器人 → 查看Webhook地址

### Q: 如何删除机器人？

A: 群设置 → 群机器人 → 长按机器人 → 移除

### Q: 能否接收用户命令？

A: 群机器人只能单向推送，无法接收消息。如需双向交互，需要创建企业微信应用（需要企业认证和管理员权限）

### Q: 发送失败怎么办？

A: 检查：
1. Webhook URL 是否正确
2. 网络连接是否正常
3. 是否超过频率限制（每分钟20条）
4. 消息内容是否超长

## 📚 对比：群机器人 vs 应用机器人

| 功能 | 群机器人 | 应用机器人 |
|------|---------|-----------|
| 权限要求 | ✅ 无需任何权限 | ❌ 需要企业认证+管理员 |
| 接入难度 | ✅ 5分钟 | ❌ 1-2天 |
| 推送消息 | ✅ 支持 | ✅ 支持 |
| 接收消息 | ❌ 不支持 | ✅ 支持 |
| @特定用户 | ❌ 不支持 | ✅ 支持 |
| 成本 | ✅ 免费 | ✅ 免费（但需要企业认证） |
| 适用场景 | 单向推送 | 复杂交互 |

## 🎓 学习要点

通过这个项目，你可以学到：

1. **Webhook原理**：理解HTTP POST请求如何发送消息
2. **API调用**：学习如何与第三方服务集成
3. **定时任务**：掌握 cron 或任务计划的使用
4. **环境变量管理**：安全地管理敏感信息
5. **错误处理**：如何优雅地处理API调用失败

---

⭐ 如有问题，欢迎提Issue！
