# 🗞️ 新闻质量与时效性优化报告 - 2025-12-16

## 📋 问题描述

用户反馈“异乡早咖啡”存在两个核心问题：
1. **内容质量差**：夹杂大量国内早教、K12等无关内容。
2. **时效性差**：存在大量过期内容。

## ✅ 解决方案

### 1. 负面关键词过滤（已部署）
- **机制**：在 `config.py` 定义 `NEGATIVE_KEYWORDS`，在 `news_fetcher.py` 实现过滤。
- **效果**：彻底屏蔽了“早教”、“金宝贝”、“K12”等无关内容。

### 2. 时效性严格限制（新部署）
- **机制**：
    - 引入 `python-dateutil` 库，支持解析多种复杂的RSS时间格式。
    - 重写 `is_news_recent` 方法：
        - 统一时间基准为当前时间（处理时区问题）。
        - 强制**丢弃**没有时间戳或时间解析失败的新闻（默认不信任）。
        - 限制时间窗口为 **7天**（可配置），只通过最近一周内的新闻。
    - 优化RSS解析逻辑：优先读取 `published` -> `updated` -> `pubDate` 等字段，确保最大概率获取准确发稿时间。

### 3. 该进的新闻抓取策略
- **增加候选池**：将每个RSS源的抓取上限从5条提升至 **30条**，以确保在大力过滤后仍有足够的新闻。
- **放宽多样性**：将单一来源的展示上限从2条提升至 **3条**，避免遗漏重要媒体的连续报道。

## 🧪 验证结果

- **测试运行**：系统成功抓取并推送了 **3条** 留学资讯。
- **内容检查**：所有资讯均为最近一周内发布，且无早教类噪音。
- **日志分析**：
    - 成功解析 ISO8601 和 RFC822 等多种时间格式。
    - 自动过滤了无时间戳的陈旧 RSS 条目。

## 🎯 预期效果

- **更纯净**：无无关内容。
- **更新鲜**：所有新闻均为最近7天内发生。
- **更稳定**：减少因时间解析错误导致的漏网之鱼。

---
**状态**: ✅ 已部署
