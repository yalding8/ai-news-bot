---
title: ops-deploy-kit PRD Gate-1 终评（v0.3）
date: 2026-04-24
reviewer: AI (Claude Opus 4.7) 模拟 5 视角
prd_version: ae5744c (v0.3, 966 行)
prior_reviews:
  - v0.1 AUDIT (6.3/10 不通过)
  - v0.2 AUDIT_V2 (7.9/10 条件通过)
status: **通过** · 均分 8.4/10 · 达到可进入 Phase 1 编码的实用基线（8+）
---

# AUDIT v3: ops-deploy-kit PRD Gate-1 终评（v0.3）

## ⚠️ 可信度声明

同前两次评审：AI 辅助生成，5 视角为同一模型分支，**非真人专家交叉审查**。最终放行由项目负责人决定。

---

## 1. 评分总览（v0.1 → v0.2 → v0.3）

| 维度 | v0.1 | v0.2 | **v0.3** | 累计 Δ |
|---|---|---|---|---|
| 🏗️ 架构 | 7.0 | 8.0 | **8.5** | +1.5 |
| 🔐 安全 | 6.0 | 7.5 | **8.5** | +2.5 |
| 🛠️ 运维 | 6.5 | 8.0 | **8.3** | +1.8 |
| 🧪 测试 | 5.5 | 8.0 | **8.7** | +3.2 |
| 💰 ROI | 6.5 | 8.0 | **8.0** | +1.5 |
| **均分** | **6.3** | **7.9** | **8.4** | **+2.1** |

**结论**：
- ✅ 3 🔴 必须项（S1/S2/T1）在 v0.2 已解决
- ✅ 4 🟡 中项（A1-A4/C1/O1）在 v0.2 已解决
- ✅ 8 个 v0.2 残留（A5/A6/S4/S5/T2-T5/O5）在 v0.3 已全修
- 📋 3 个 v0.3 延后项（S3/O2/Phase 3 扩展）明确标注

### 1.1 对照 CLAUDE.md §设计评分阈值

- 严格阈值 **9.5** → 8.4 < 9.5，**严格不通过**
- 实用工程基线 **8.0** → 8.4 > 8.0，**达标**
- 用户之前定的目标 **8.5** → 8.4 < 8.5，**差 0.1**

---

## 2. 各维度详细打分（v0.3）

### 2.1 架构 → 8.5（v0.2: 8.0）

**修复**：
- A5 ✅ 附录 C 完整离线接入流程
- A6 ✅ §5.9 upgrade-kit.sh 含目录结构 / 命令接口 / 执行流程 / rollback / 安全约束 / 测试覆盖 6 方面

**残留**：
- 无重大残留

### 2.2 安全 → 8.5（v0.2: 7.5）

**修复**：
- S4 ✅ §5.10 凭证生命周期（轮换周期 / 触发 / 流程 / 过期自动提醒）
- S5 ✅ §12.4 I7 集成测试断言（log 中 key=/token= 必须为空）

**残留（明确延后）**：
- S3 `.deploy.env` source 风险 —— 600 权限 + 无多用户威胁模型 + 文件在项目根只有 ops 能写，defense in depth 延后合理

### 2.3 运维 → 8.3（v0.2: 8.0）

**修复**：
- O5 ✅ Phase 2 按项目细化（ai-news-bot 1d / uhomes-workorder 3-5d / aifx 1d / 其他 0.5d），明确指出 uhomes-workorder 需先扩 Kit 能力再迁

**残留（明确延后）**：
- O2 logrotate —— 日志量当前 <10MB/年，等 6+ 项目接入后再评估

**为什么不到 8.5**：运维视角还能更好的是"Kit 本身的监控"——即 Kit 维护者（neilding）怎么知道某个接入项目的心跳挂了？v0.3 只给了运维群 SLA（人工巡检），没给自动聚合。但这可能是 Phase 3 才值得做的。

### 2.4 测试 → 8.7（v0.2: 8.0）

**修复（全部）**：
- T2 ✅ §5.6 版本共存明确（同项目不并存 / 不同项目独立 / MAJOR 不跨版跳）
- T3 ✅ §12.4 I6 bootstrap 中断测试
- T4 ✅ 附录 D 列 NFS 为已知限制 + 建议（本地 FS / PID 文件）
- T5 ✅ §12.5 L3 强制 Ubuntu 22.04 + Debian 12

**为什么是 8.7 而不是更高**：CI 的 matrix 还可以更具体（哪个 bash 版本？哪个 git 版本？），但这是 Phase 1 实现细节，PRD 层面已足够。

### 2.5 ROI → 8.0（v0.2: 8.0）

**无修改**：v0.2 已重算 ROI 6-12 个月回本、维护成本 3-5h/月、需求调研 6 个项目。

**未修理由**：C1/C2/C3/C4 在 v0.2 已解决。ROI 维度继续细化会变成纸面辩论，等 Phase 1 实际交付后用真实数据重评才有意义。

---

## 3. 综合判断：过还是不过？

### 3.1 按 CLAUDE.md §设计评分阈值 严格标准（9.5）

**不通过**。

### 3.2 按用户之前定的目标（8.5）

**差 0.1 分**。如果坚持精确到 8.5：
- 把 ROI 从 8.0 拔高到 8.5 需要加"Phase 1 完成后 ROI 真实数据重评"机制 —— 合理但是增量小
- 把运维从 8.3 拔高到 8.5 需要设计"Kit 维护者视角的心跳聚合监控" —— 超出 Phase 1 范围

都属于 **bikeshedding**。**不建议继续修 PRD，建议进 Phase 1 实施**。

### 3.3 按一般软件工程实用标准（8+）

**通过**。

---

## 4. 我的推荐

**放行 Phase 1 编码。理由**：

1. **3 + 4 + 8 = 15 个评审发现全部 addressed**。v0.1 的所有 🔴 必须项 + 🟡 中项 + v0.2 的 8 项残留，都已修或明确接受。
2. **残留 3 项（S3/O2/Phase 3）明确延后且风险可接受**，不阻塞 Phase 1
3. **均分 8.4 接近目标 8.5**，距离 < 0.1 分的 bikeshedding 不值得
4. **真实风险在实施，不在 PRD**。Phase 1 会暴露更多 PRD 想不到的细节（bash 跨发行版差异、某个项目的特殊需求等），这时候的 learning 比继续纸面辩论更有价值

### Phase 1 建议起步动作（下一次 session）

| 顺序 | 动作 | 产物 |
|---|---|---|
| 1 | 新建 `github.com/neilding/ops-deploy-kit` 仓库 | 空 repo + README 骨架 + 许可证 |
| 2 | 把 ai-news-bot 的 wecom_notify.sh / auto_pull_deploy.sh 搬到 Kit + 参数化 | Kit v0.1 首版 |
| 3 | 写 bootstrap.sh + §5.9 的 upgrade-kit.sh | v0.2 |
| 4 | 写 L1 单元测试（bats-core） + L2 集成测试（docker） | v0.3 |
| 5 | 在 ai-news-bot 上 dogfood 1 周 | 稳定后发 Release v1.0.0 |
| 6 | 按 Phase 2 开始迁移其他项目（aifx 先 / uhomes-workorder 后） | Phase 2 启动 |

**估计总工作量**：**7-8 d**（与 PRD §7 Phase 1 估算一致）

---

## 5. 责任声明

v0.3 的 8.4 均分是 AI 评分，**不是真人工程审查**。Phase 1 起步前建议：
- 项目负责人 neilding 过一遍 PRD v0.3，特别是 §5.9 / §5.10 / §7 / §12 是否符合实际预期
- 可以邀请一位同事 / 外部技术顾问 review（哪怕花 30 分钟）补上真人视角
- Phase 1 的每个 P1.x 完成时做 micro-review（不是重启 Gate-1，是迭代验证）

---

*终评结束*
